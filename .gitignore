node_modules/
dist/
*.log

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Publish plumbing — local only. Neither holds a secret VALUE (.npmrc expands
# ${NPM_TOKEN} from the environment at run time), but this repo is PUBLIC and
# naming which credentials exist, and the vault they come from, is free
# reconnaissance for no benefit to anyone reading the source.
.npmrc
.env.refs

# Agent/contributor guidance for OUR sessions. README.md is the public document;
# this one mostly helps us, and it is the file that leaked internal architecture
# once already. Keeping it untracked removes the class of mistake rather than
# relying on remembering to sanitise it.
CLAUDE.md